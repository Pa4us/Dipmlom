@{ Layout = "_Layout"; ViewData["Title"] = "Доступ запрещён"; }

<div class="text-center py-5">
    <i class="bi bi-shield-lock text-danger" style="font-size:4rem"></i>
    <h3 class="mt-3 fw-bold">Доступ запрещён</h3>
    <p class="text-muted">У вас нет прав для просмотра этой страницы.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-2">
        <i class="bi bi-arrow-left me-1"></i> На главную
    </a>
</div>
